<!DOCTYPE SimulIDE>

<compiler name="Avra" inclPath="" type="avrasm">
    <step 
        command="avra"
        arguments=" -W NoRegDef -l $fileDir$fileName.lst -I $inclPath $filePath"
    />
</compiler>
