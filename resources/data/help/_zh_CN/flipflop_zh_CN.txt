- 对Set/Reset引脚取反: (是)
   对Set和Reset引脚取反.

- 对Clock引脚取反: (否)
   对Clock引脚取反.

- 触发类型: (Clock)
   "Clock(时钟)"上升沿触发.
   "enable(启用)"双边沿触发.
   "None(无)"隐藏Clock引脚.

电气特性:
#include logicinputs
#include logicoutputs
#include logicedges
